<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dimas Dzikra Pratama | Portfolio</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Oswald:wght@500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- 1. SETUP & VARIABLES --- */
        html { scroll-behavior: smooth; }

        :root {
            /* ENGINEER PALETTE (Biru Profesional) */
            --primary: #2563eb;       
            --primary-dark: #1e40af;
            --accent: #0ea5e9;
            --bg-body: #ffffff;
            --bg-section: #f8fafc;
            --text-main: #0f172a;     
            --text-muted: #64748b;
            --border: #0f172a;        
            --card-bg: #ffffff;
            --sidebar-width: 450px;
        }

        [data-theme="dark"] {
            --primary: #3b82f6;
            --primary-dark: #60a5fa;
            --accent: #38bdf8;
            --bg-body: #020617;       
            --bg-section: #0f172a;
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
            --border: #f8fafc;        
            --card-bg: #1e293b;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-body);
            color: var(--text-main);
            overflow-x: hidden;
            transition: background 0.3s, color 0.3s;
        }

        h1, h2, h3, .marquee-text, .nav-logo, .sidebar-link {
            font-family: 'Oswald', sans-serif;
            text-transform: uppercase;
        }

        /* --- 2. NAVBAR --- */
        .navbar {
            position: fixed; top: 0; width: 100%;
            padding: 20px 40px;
            display: flex; justify-content: space-between; align-items: center;
            background: rgba(var(--bg-body), 0.8);
            backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 2px solid var(--border);
        }

        .nav-logo { font-size: 1.5rem; font-weight: 700; letter-spacing: 1px; z-index: 3001; }
        
        .nav-actions { display: flex; gap: 15px; align-items: center; z-index: 3001; }
        
        .icon-btn {
            background: none; border: 2px solid var(--border);
            color: var(--text-main); padding: 8px 16px;
            cursor: pointer; font-weight: bold; font-family: 'Oswald';
            transition: all 0.2s;
            display: flex; align-items: center; gap: 8px;
        }
        .icon-btn:hover { background: var(--text-main); color: var(--bg-body); }

        /* --- 3. SIDEBAR MENU (Leclerc Style) --- */
        .menu-overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.6);
            backdrop-filter: blur(5px);
            z-index: 2000;
            opacity: 0; pointer-events: none; transition: opacity 0.4s ease;
        }
        .menu-overlay.active { opacity: 1; pointer-events: all; }

        .sidebar {
            position: fixed; top: 0; right: -100%;
            width: var(--sidebar-width); height: 100vh;
            background: var(--bg-body);
            border-left: 2px solid var(--border);
            z-index: 2001;
            padding: 100px 50px;
            display: flex; flex-direction: column; justify-content: center;
            transition: right 0.5s cubic-bezier(0.77, 0, 0.175, 1);
        }
        .sidebar.active { right: 0; }

        .sidebar-header {
            position: absolute; top: 30px; left: 50px;
            font-family: 'Inter'; font-size: 0.8rem; letter-spacing: 2px;
            color: var(--text-muted); text-transform: uppercase;
        }

        .sidebar-links { list-style: none; }
        .sidebar-item { margin-bottom: 25px; opacity: 0; transform: translateX(20px); transition: 0.3s; }
        
        .sidebar.active .sidebar-item { opacity: 1; transform: translateX(0); }
        .sidebar.active .sidebar-item:nth-child(1) { transition-delay: 0.1s; }
        .sidebar.active .sidebar-item:nth-child(2) { transition-delay: 0.2s; }
        .sidebar.active .sidebar-item:nth-child(3) { transition-delay: 0.3s; }
        .sidebar.active .sidebar-item:nth-child(4) { transition-delay: 0.4s; }
        .sidebar.active .sidebar-item:nth-child(5) { transition-delay: 0.5s; }

        .sidebar-link {
            text-decoration: none; color: var(--text-main);
            font-size: 2.5rem; line-height: 1;
            display: flex; align-items: baseline; gap: 20px;
            transition: color 0.3s;
            position: relative;
        }

        .link-num { font-family: 'Inter'; font-size: 1rem; color: var(--primary); font-weight: bold; }

        .sidebar-link:hover { color: var(--primary); }
        .sidebar-link::after {
            content: ''; position: absolute; left: 0; top: 50%;
            width: 0; height: 3px; background: var(--primary);
            transition: width 0.3s ease;
        }
        .sidebar-link:hover::after { width: 100%; }

        .close-btn {
            position: absolute; top: 20px; right: 40px;
            background: none; border: none;
            font-size: 2rem; color: var(--text-main);
            cursor: pointer;
        }

        /* --- 4. HERO SECTION (3D Tilt) --- */
        .hero {
            min-height: 100vh;
            display: flex; flex-direction: column;
            align-items: center; justify-content: center;
            text-align: center; padding: 120px 20px 40px;
            position: relative;
        }

        .hero-img-container {
            width: 250px; height: 250px; margin-bottom: 30px;
            position: relative; border-radius: 50%;
            perspective: 1000px; transform-style: preserve-3d;
            cursor: pointer;
        }

        .hero-img {
            width: 100%; height: 100%; object-fit: cover;
            border-radius: 50%; border: 4px solid var(--border);
            box-shadow: 10px 10px 0px var(--primary);
            transition: transform 0.1s;
        }

        .hero h1 {
            font-size: 4rem; line-height: 0.95; margin-bottom: 20px;
            text-shadow: 3px 3px 0px var(--text-muted);
        }
        
        .hero p { font-size: 1.2rem; max-width: 700px; margin: 0 auto 30px; font-family: 'Inter'; color: var(--text-muted); }

        .cta-btn {
            padding: 15px 40px; background: var(--primary); color: white;
            font-family: 'Oswald', sans-serif; font-size: 1.2rem;
            text-decoration: none; border: 2px solid var(--border);
            box-shadow: 5px 5px 0px var(--text-main);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .cta-btn:hover { transform: translate(-2px, -2px); box-shadow: 7px 7px 0px var(--text-main); }

        /* --- 5. MARQUEE --- */
        .marquee-container {
            background: var(--primary); color: white; padding: 20px 0;
            overflow: hidden; border-top: 2px solid var(--border);
            border-bottom: 2px solid var(--border); white-space: nowrap;
            transform: rotate(-1deg); width: 105%; margin-left: -2.5%; margin-bottom: 60px;
        }
        .marquee-content { display: inline-block; animation: scroll 30s linear infinite; }
        .marquee-text { font-size: 3rem; margin-right: 50px; font-weight: 700; display: inline-block; }
        @keyframes scroll { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }

        /* --- 6. GRID SYSTEM (Projects) --- */
        .section-container { max-width: 1200px; margin: 0 auto; padding: 40px 20px; }
        .section-header { 
            font-size: 3rem; margin-bottom: 40px; 
            border-left: 10px solid var(--primary); padding-left: 20px;
        }

        .bento-grid {
            display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 25px;
        }

        .card {
            background: var(--card-bg); border: 2px solid var(--border);
            transition: all 0.3s; cursor: pointer; overflow: hidden;
            display: flex; flex-direction: column; height: 100%;
        }
        .card:hover { transform: translateY(-5px); box-shadow: 8px 8px 0px var(--text-muted); }
        
        .card-body { padding: 25px; flex-grow: 1; display: flex; flex-direction: column; justify-content: space-between; }
        .card-title { font-size: 1.5rem; margin-bottom: 10px; line-height: 1.2; }
        .card-tech { font-size: 0.8rem; color: var(--primary); font-weight: bold; text-transform: uppercase; margin-bottom: 15px; display: block; }
        .card-desc { font-size: 0.95rem; color: var(--text-muted); line-height: 1.6; }

        /* --- 7. FOOTER --- */
        footer { background: var(--bg-section); border-top: 2px solid var(--border); padding: 80px 20px; margin-top: 80px; }
        .footer-content { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 40px; }
        .footer-links a { display: block; font-family: 'Oswald'; font-size: 2.5rem; color: var(--text-main); text-decoration: none; margin-bottom: 10px; transition: 0.3s; }
        .footer-links a:hover { color: var(--primary); padding-left: 15px; }
        .social-bar { display: flex; gap: 20px; margin-top: 40px; font-size: 2rem; }
        .social-icon { color: var(--text-main); transition: 0.2s; }
        .social-icon:hover { color: var(--primary); transform: scale(1.2); }

        /* --- 8. MODAL --- */
        .modal { display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.9); z-index: 4000; justify-content: center; align-items: center; padding: 20px; opacity: 0; transition: opacity 0.3s; }
        .modal.active { display: flex; opacity: 1; }
        .modal-content { background: var(--bg-body); width: 100%; max-width: 800px; border: 2px solid var(--border); max-height: 90vh; overflow-y: auto; padding: 40px; position: relative; }
        .close-modal-content { position: absolute; top: 15px; right: 20px; font-size: 2rem; cursor: pointer; color: var(--text-main); }
        
        @media (max-width: 768px) {
            .sidebar { width: 100%; } 
            .hero h1 { font-size: 3rem; }
            .footer-content { grid-template-columns: 1fr; }
            .nav-logo { font-size: 1.2rem; }
        }
    </style>
</head>
<body>

    <nav class="navbar">
        <div class="nav-logo">DIMAS<span style="color:var(--primary)">.ENG</span></div>
        <div class="nav-actions">
            <button class="icon-btn" id="lang-toggle">ID</button>
            <button class="icon-btn" id="theme-toggle"><i class="fas fa-moon"></i></button>
            <button class="icon-btn" onclick="toggleSidebar()" style="background:var(--primary); color:white; border-color:var(--primary);">
                MENU <i class="fas fa-bars"></i>
            </button>
        </div>
    </nav>

    <div class="menu-overlay" id="overlay" onclick="toggleSidebar()"></div>
    <div class="sidebar" id="sidebar">
        <button class="close-btn" onclick="toggleSidebar()">&times;</button>
        <div class="sidebar-header">SELECT CHAPTER</div>
        
        <ul class="sidebar-links">
            <li class="sidebar-item"><a href="#hero" class="sidebar-link" onclick="toggleSidebar()"><span class="link-num">01</span> PROFILE</a></li>
            <li class="sidebar-item"><a href="#about" class="sidebar-link" onclick="toggleSidebar()"><span class="link-num">02</span> ABOUT</a></li>
            <li class="sidebar-item"><a href="#projects" class="sidebar-link" onclick="toggleSidebar()"><span class="link-num">03</span> PROJECTS</a></li>
            <li class="sidebar-item"><a href="#certificates" class="sidebar-link" onclick="toggleSidebar()"><span class="link-num">04</span> CERTIFICATES</a></li>
            <li class="sidebar-item"><a href="#contact" class="sidebar-link" onclick="toggleSidebar()"><span class="link-num">05</span> CONTACT</a></li>
        </ul>
    </div>

    <section class="hero" id="hero">
        <div class="hero-img-container" id="hero-tilt">
            <img src="https://ui-avatars.com/api/?name=Dimas+Dzikra&background=random&size=300&bold=true" alt="Dimas Dzikra" class="hero-img">
        </div>
        <h1 id="hero-title">DIMAS DZIKRA<br>PRATAMA</h1>
        <p data-i18n="tagline">Bachelor of Applied Computer Engineering Technology | IPB University | IoT & Embedded System Enthusiast</p>
        <a href="#projects" class="cta-btn" data-i18n="btnProjects">VIEW PROJECTS</a>
    </section>

    <div class="marquee-container">
        <div class="marquee-content">
            <span class="marquee-text">EMBEDDED SYSTEMS • IOT SOLUTIONS • MACHINE LEARNING • MICROCONTROLLER • PYTHON • C++ • </span>
            <span class="marquee-text">EMBEDDED SYSTEMS • IOT SOLUTIONS • MACHINE LEARNING • MICROCONTROLLER • PYTHON • C++ • </span>
        </div>
    </div>

    <section class="section-container" id="about">
        <h2 class="section-header" data-i18n="navAbout">ABOUT ME</h2>
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 40px; align-items: start;">
            <div style="font-size: 1.15rem; line-height: 1.8;">
                <p data-i18n="aboutText">
                    Bachelor of Applied Computer Engineering Technology from IPB University with a CGPA of 3.60/4.00. 
                    I specialize in building automated systems, IoT integrations, and hardware-software bridging.
                    Experienced in coordinating student-led events and managing technical divisions.
                </p>
                <br>
                <div style="display: flex; gap: 10px; flex-wrap: wrap;">
                    <span style="background:var(--primary); color:white; padding: 5px 10px; font-weight:bold;">C / C++</span>
                    <span style="background:var(--primary); color:white; padding: 5px 10px; font-weight:bold;">PYTHON</span>
                    <span style="background:var(--primary); color:white; padding: 5px 10px; font-weight:bold;">IOT</span>
                    <span style="background:var(--primary); color:white; padding: 5px 10px; font-weight:bold;">NETWORKING</span>
                </div>
            </div>
            <div>
                <img src="https://images.unsplash.com/photo-1517077304055-6e89abbf09b0?auto=format&fit=crop&w=600&q=80" style="width:100%; border: 2px solid var(--border); box-shadow: 10px 10px 0px var(--text-muted);">
            </div>
        </div>
    </section>

    <section class="section-container" id="projects">
        <h2 class="section-header" data-i18n="navProjects">PROJECTS</h2>
        <div class="bento-grid" id="project-grid">
            </div>
    </section>

    <section class="section-container" id="certificates">
        <h2 class="section-header" data-i18n="navCert">CERTIFICATES</h2>
        <div class="bento-grid" id="cert-grid">
            </div>
    </section>

    <footer id="contact">
        <div class="footer-content">
            <div class="footer-links">
                <a href="#hero">HOME</a>
                <a href="#about">ABOUT</a>
                <a href="#projects">PROJECTS</a>
                <a href="mailto:dimasdzikra@apps.ipb.ac.id">EMAIL ME</a>
            </div>
            <div>
                <h3 style="font-size: 2rem;">CONNECT</h3>
                <div class="social-bar">
                    <a href="https://linkedin.com/in/dimasdzikra" target="_blank" class="social-icon"><i class="fab fa-linkedin"></i></a>
                    <a href="mailto:dimasdzikra@apps.ipb.ac.id" class="social-icon"><i class="fas fa-envelope"></i></a>
                    <a href="#" class="social-icon"><i class="fab fa-github"></i></a>
                </div>
                <p style="margin-top: 30px; color: var(--text-muted);">&copy; 2025 Dimas Dzikra Pratama.</p>
            </div>
        </div>
    </footer>

    <div id="modal" class="modal">
        <div class="modal-content">
            <span class="close-modal-content" onclick="closeModalFunc()">&times;</span>
            <h2 id="m-title" style="margin-bottom:10px; font-family:'Oswald'; font-size:2rem; color:var(--primary);">Title</h2>
            <div style="margin-bottom:20px; border-bottom:1px solid var(--border); padding-bottom:15px; font-size: 0.9rem;">
                <span id="m-tools" style="font-weight:bold; text-transform:uppercase;">...</span>
            </div>
            <p id="m-desc" style="line-height:1.8; font-size:1.1rem;">Description...</p>
        </div>
    </div>

    <script>
        // --- DATA PORTFOLIO (HARDCODED FROM CV) ---
        // Data Projects
        const projectsData = [
            {
                id: 1,
                title: "Anti-Theft Detection System using PIR Sensor",
                tools: "IoT • Firebase • Web Dashboard",
                desc_en: "Built an IoT-based security system using a PIR sensor to detect human motion in real time. Integrated Firebase Realtime Database for continuous monitoring and logging. Developed a web dashboard to visualize alerts and timestamps.",
                desc_id: "Membangun sistem keamanan berbasis IoT menggunakan sensor PIR untuk mendeteksi gerakan manusia secara real-time. Terintegrasi dengan Firebase untuk pemantauan berkelanjutan. Mengembangkan dashboard web untuk visualisasi peringatan."
            },
            {
                id: 2,
                title: "Automatic Earthquake Detector",
                tools: "ADXL355 Sensor • Microcontroller",
                desc_en: "Developed a vibration alert system using ADXL355 accelerometer to detect abnormal motion and provide early warnings. Analyzed vibration patterns to identify indicators of hazardous conditions.",
                desc_id: "Mengembangkan sistem peringatan getaran menggunakan accelerometer ADXL355 untuk mendeteksi gerakan abnormal dan memberikan peringatan dini. Menganalisis pola getaran untuk identifikasi kondisi berbahaya."
            },
            {
                id: 3,
                title: "Object Counter System",
                tools: "IR Sensor • IC 4026 • 7-Segment",
                desc_en: "Built an automated object-counting system using IR sensor and IC 4026. Simulated in Proteus before hardware implementation. Developed a conveyor-based prototype for accurate object detection.",
                desc_id: "Membangun sistem penghitung objek otomatis menggunakan sensor IR dan IC 4026. Disimulasikan di Proteus sebelum implementasi perangkat keras. Mengembangkan prototipe berbasis konveyor."
            },
            {
                id: 4,
                title: "Electric Piano using NE555",
                tools: "NE555 Timer • Analog Electronics",
                desc_en: "Designed and assembled an electronic piano circuit using the NE555 timer to generate distinct audio frequencies. Optimized component configuration for stable tone output.",
                desc_id: "Merancang dan merakit sirkuit piano elektronik menggunakan timer NE555 untuk menghasilkan frekuensi audio yang berbeda. Konfigurasi komponen dioptimalkan untuk output nada yang stabil."
            }
        ];

        // Data Certificates
        const certsData = [
            {
                id: 101,
                title: "CCNA: Switching, Routing, and Wireless Essentials",
                issuer: "Cisco (2024)",
                desc: "Certified proficiency in switching, routing protocols, and wireless network configurations."
            },
            {
                id: 102,
                title: "CCNA: Introduction to Networks",
                issuer: "Cisco (2023)",
                desc: "Foundation knowledge of network architectures, models, protocols, and networking elements."
            }
        ];

        // --- TRANSLATION & THEME LOGIC ---
        const translations = {
            en: {
                tagline: "Bachelor of Applied Computer Engineering Technology | IPB University | IoT & Embedded System Enthusiast",
                btnProjects: "VIEW PROJECTS",
                navAbout: "ABOUT ME",
                navProjects: "PROJECTS",
                navCert: "CERTIFICATES",
                aboutText: "Bachelor of Applied Computer Engineering Technology from IPB University with a CGPA of 3.60/4.00. I specialize in building automated systems, IoT integrations, and hardware-software bridging. Experienced in coordinating student-led events and managing technical divisions."
            },
            id: {
                tagline: "Sarjana Terapan Teknologi Rekayasa Komputer | IPB University | IoT & Embedded System Enthusiast",
                btnProjects: "LIHAT PROYEK",
                navAbout: "TENTANG SAYA",
                navProjects: "PROYEK",
                navCert: "SERTIFIKAT",
                aboutText: "Sarjana Terapan Teknologi Rekayasa Komputer dari IPB University dengan IPK 3.60/4.00. Saya berspesialisasi dalam membangun sistem otomatisasi, integrasi IoT, dan jembatan hardware-software. Berpengalaman dalam mengoordinasikan acara mahasiswa dan manajemen divisi teknis."
            }
        };

        let currentLang = 'id'; // Default Bahasa Indonesia
        let currentTheme = 'light';

        const htmlEl = document.documentElement;
        const themeBtn = document.getElementById('theme-toggle');
        const langBtn = document.getElementById('lang-toggle');
        const modal = document.getElementById('modal');

        function init() {
            setTheme(currentTheme);
            setLanguage(currentLang);
            renderProjects();
            renderCerts();
        }

        function setTheme(theme) {
            htmlEl.setAttribute('data-theme', theme);
            currentTheme = theme;
            themeBtn.innerHTML = theme === 'dark' ? '<i class="fas fa-sun"></i>' : '<i class="fas fa-moon"></i>';
        }

        function setLanguage(lang) {
            currentLang = lang;
            langBtn.textContent = lang === 'en' ? 'ID' : 'EN';
            
            document.querySelectorAll('[data-i18n]').forEach(el => {
                const key = el.getAttribute('data-i18n');
                if (translations[lang][key]) el.innerText = translations[lang][key];
            });
            renderProjects(); // Re-render text inside cards
        }

        themeBtn.onclick = () => setTheme(currentTheme === 'light' ? 'dark' : 'light');
        langBtn.onclick = () => setLanguage(currentLang === 'en' ? 'id' : 'en');

        // --- RENDER FUNCTIONS ---
        function renderProjects() {
            const grid = document.getElementById('project-grid');
            grid.innerHTML = projectsData.map(p => `
                <div class="card" onclick="openModal('project', ${p.id})">
                    <div class="card-body">
                        <div>
                            <h3 class="card-title">${p.title}</h3>
                            <span class="card-tech">${p.tools}</span>
                        </div>
                        <p class="card-desc">
                            ${currentLang === 'en' ? p.desc_en : p.desc_id}
                        </p>
                    </div>
                </div>
            `).join('');
        }

        function renderCerts() {
            const grid = document.getElementById('cert-grid');
            grid.innerHTML = certsData.map(c => `
                <div class="card" style="cursor:default;">
                    <div class="card-body">
                        <h3 class="card-title">${c.title}</h3>
                        <span class="card-tech">${c.issuer}</span>
                        <p class="card-desc">${c.desc}</p>
                    </div>
                </div>
            `).join('');
        }

        // --- MODAL LOGIC ---
        window.openModal = function(type, id) {
            if(type !== 'project') return; // Only projects have modal details
            const data = projectsData.find(p => p.id === id);
            
            document.getElementById('m-title').innerText = data.title;
            document.getElementById('m-tools').innerText = data.tools;
            document.getElementById('m-desc').innerText = currentLang === 'en' ? data.desc_en : data.desc_id;
            
            modal.classList.add('active');
        };

        function closeModalFunc() { modal.classList.remove('active'); }
        modal.onclick = (e) => { if (e.target === modal) modal.classList.remove('active'); };

        function toggleSidebar() {
            document.getElementById('sidebar').classList.toggle('active');
            document.getElementById('overlay').classList.toggle('active');
        }

        // --- 3D TILT EFFECT ---
        const heroContainer = document.getElementById('hero-tilt');
        const heroImg = heroContainer.querySelector('img');

        heroContainer.addEventListener('mousemove', (e) => {
            const rect = heroContainer.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;
            const xPct = (x / rect.width) - 0.5;
            const yPct = (y / rect.height) - 0.5;
            heroImg.style.transform = `rotateX(${yPct * -20}deg) rotateY(${xPct * 20}deg) scale(1.05)`;
        });

        heroContainer.addEventListener('mouseleave', () => {
            heroImg.style.transform = `rotateX(0deg) rotateY(0deg) scale(1)`;
        });

        // Run
        init();
    </script>
</body>
</html>


    <link rel="icon" href="LOGOFULL.png" type="image/png">